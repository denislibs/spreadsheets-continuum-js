Read AGENTS.md before writing any code — this project uses Continuum
(`@continuum-js/*`), which is NOT React: components run once, there are no
hooks, and the file lists exact API signatures and the mistakes to avoid.
